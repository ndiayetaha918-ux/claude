name = "drafter-worker"
main = "worker.js"
compatibility_date = "2024-09-23"

# Optionnel : pour cacher les fiches TM 24h (réduit le scraping)
# Crée un KV namespace dans le dashboard CF, colle l'ID ci-dessous.
# [[kv_namespaces]]
# binding = "TM_CACHE"
# id = "<paste_kv_id>"

# Secret à définir AVANT le déploiement :
#   wrangler secret put ANTHROPIC_API_KEY
